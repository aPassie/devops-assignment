# Exit code 0 with restartPolicy Never => Pod phase Succeeded (Completed).
apiVersion: v1
kind: Pod
metadata:
  name: lifecycle-succeeded
  labels:
    demo: lifecycle
spec:
  restartPolicy: Never
  containers:
    - name: busybox
      image: busybox
      command: ["sh", "-c", "echo done"]
